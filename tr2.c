#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a;
	int b;
	printf(" nhap 2 so bat ki\n");
	scanf("%d %d",&a,&b);
	if(a=b)
	printf("difference is equal to values\n");
	else
	printf("difference is not equal to any of the values");
	return 0;
}
